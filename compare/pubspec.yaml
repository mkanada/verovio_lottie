name: compare
description: "Comparacao visual SVG vs. dotLottie do verovio_lottie: SVG via resvg (compare/svg_render/), dotLottie via dotlottie_flutter FFI."
publish_to: 'none'
version: 0.2.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  dotlottie_flutter: ^0.1.7
  image: ^4.5.4
  archive: ^4.0.7
  args: ^2.7.0
  ffi: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
